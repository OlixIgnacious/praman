# Pending manual runs

## Done

Everything through the full eval + fix + re-test cycle is deployed and verified live — final score **11/12** (up from 7/12), zero regressions. See `eval/results.md` for the full comparison, `TRACKER.md`/`plan.md` for status.

## Nothing currently pending

One open decision, not a pending run: whether to pursue the remaining `correct_but_anomalous` false positive (a real detector-calibration gap — book-wide vs. per-counterparty scale baseline, two fixes identified in `eval/results.md`, both genuine detector/schema work) before submission, or accept 11/12 with the gap documented. Only remaining build phase either way: **Days 17–18 — Rehearse & submit**.
