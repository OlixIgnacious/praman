# Pending manual runs

## Done

Everything through Days 15–17 is deployed, verified, and scored live — including the full eval pass (`eval/results.md`: 12/12 cases run, 7/12 correct, governance gate 100%, every miss root-caused). See `TRACKER.md`/`plan.md`.

## Nothing currently pending

One open decision, not a pending run: whether to apply the 5 fixes `eval/results.md` identified (all low-risk — orchestration wording, one `LINE_ITEM_MAP` approval, one `LINE_ITEM_ID` schema/dedup-key correction, one `CREDIT_EXPOSURE_SV` column addition) before submission, or carry the current 7/12 score with documented fixes into the pitch as-is. Only remaining build phase either way: **Days 17–18 — Rehearse & submit**.
